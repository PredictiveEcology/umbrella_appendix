# Scenario 10\% Area Chosen

A scenario where stuff happened (Figure \@ref(fig:scenario10-plot1)).

```{r scenario10-plot1, fig.cap='Here is a nice scenario10 figure!', out.width='80%', fig.asp=.75, fig.align='center', fig.alt='Plot with connected points showing that vapor pressure of mercury increases exponentially as temperature increases.'}
par(mar = c(4, 4, .1, .1))
plot(pressure, type = 'b', pch = 19)
```

And also other things happened (Table \@ref(tab:scenario10-table1)).

```{r scenario10-table1, tidy=FALSE}
knitr::kable(
  head(pressure, 10), caption = 'Here is a nice table!',
  booktabs = TRUE
)
```
